@model IEnumerable<Entity_Framework.Models.Student>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.student_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.student_dob)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.student_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.student_dob)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.student_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.student_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.student_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
